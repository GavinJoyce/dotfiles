alias sl="cat ~/dev/dotfiles/learning.txt"

alias dot="~/dev/dotfiles"
alias dev="~/dev"
alias os="~/dev/opensource"
alias rp="~/dev/RestPack"
alias rps="~/dev/RestPack/restpack-serializer"
alias i="~/dev/intercom"
alias io="~/dev/intercom/intercom"
alias ioc="~/dev/intercom/intercom-core"
alias iog="~/dev/intercom/intercom-gem"
alias ioi="~/dev/intercom/intercom-instrumentation"
alias iot="~/dev/intercom/intercom-testing"
alias iou="~/dev/intercom/intercom-user-service"
alias em="~/dev/intercom/embercom"
alias emc="~/dev/intercom/embercom-composer"
alias buios="bundle update intercom-user-service"

alias hs=history
alias h="cd ~/"
alias reload="source ~/.zshrc"
alias r!='reload'
alias r1='reload'
alias c="clear"
alias er="ember server"
alias ni="npm install"
alias bi="bower install"
alias nbi="npm install && bower install"

alias atom="/Applications/Atom.app/Contents/Resources/app/atom.sh"
alias a="atom ."
alias v="vim"

alias ,ev="vim -c \"execute 'normal ,ev'\""
alias ,el="vim -c \"execute 'normal ,el'\""
alias ,ea="vim -c \"execute 'normal ,ea'\""

alias st="rake start"
alias rt="rake -T"
alias rtg="rake -T | grep"
alias re="rbenv"

alias glol="git log --pretty=oneline --abbrev-commit --graph --decorate"
alias gll="glol"
alias grl="git reflog"
alias gst="git status"
alias grh="git reset --hard"
alias s="git status --short"
alias ss="git status"
alias d="git diff"
alias dc="git diff --cached"
alias gcob="git checkout -B"
alias gcom="git checkout master"
alias gcop="git checkout -"
alias go="git checkout -"
alias gphm="git push heroku master"
alias grm="git rebase -i master"
alias gra="git rebase --abort"
alias grc="git rebase --continue"
alias ga="git add . && gst"
alias gcm="git commit -m"
alias gbc="git branch --merged master | grep -v master | xargs git branch -d"
alias gpf="git push --force"
alias gs="git show"
alias spruce="git branch --merged master | grep -v 'master' | xargs -n 1 git branch -d"
alias gpine="spruce"
alias asdf="python -m SimpleHTTPServer"

gac() { git add . && git commit -m "$*" }
gaf() { git add . && git commit -am "fixup! $*" }
gas() { git add . && git commit -am "squash! $*" }

function hr { printf '=%.0s' $(seq $COLUMNS); }

alias csha="~/dev/dotfiles/scripts/csha.rb"
alias cbn="~/dev/dotfiles/scripts/cbn.rb"
alias statsd="~/dev/dotfiles/scripts/statsd.rb"

function fsha() {
	if [[ "$@" != "" ]]; then
		for hash in $@;do
			echo $hash;
	git branch -a --contains $hash | grep 'master\|staging\|production';\
		done;
	else
		echo "You must give a hash argument.";
	fi
}

function mk() {
  if [ ! -n "$1" ]; then
    echo "Enter a directory name"
	elif [ -d $1 ]; then
    echo "\`$1' already exists"
  else
    mkdir $1 && cd $1
  fi
}

alias b2="bundle --local"
alias bl="bundle --local"
alias be="bundle exec"
